# Fallback Source Search Notes

This note records the bounded post-calibration source-search pass completed after the `0.05`-step sort-metric sweep failed to produce an acceptable publication-grade control.

## Search outcome

The fallback pass did not yield a new screenable publication-grade candidate. The three bounded attempts were:

| Candidate | Institution path | Outcome |
| --- | --- | --- |
| `PPN832871516` | GDZ IIIF | Reached candidate-identification stage only. The GDZ manifest host reset connections from the workspace during acquisition, so no local `61`-page screening run could be completed. |
| `ti-kapsel-2-13s` | HAB | Rejected at metadata review. HAB bibliographic description identifies the item as `1689`, outside the required `1600–1620` window. |
| `229-19-theol-6s` | HAB | Rejected at metadata review. HAB bibliographic description identifies the item as `1662`, outside the required `1600–1620` window. |

## Interpretation

This bounded fallback pass did not overturn the calibration result. The practical blocker is no longer just "find another random early modern witness." The current component set and thresholds remain insufficiently discriminating, and candidate discovery/acquisition is still brittle enough that source choice alone cannot be treated as the sole problem.
